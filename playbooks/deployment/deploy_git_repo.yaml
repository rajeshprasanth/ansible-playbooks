---
- name: Clone and deploy Git repository
  hosts: all
  become: yes

  vars:
    repo_url: "https://github.com/example/project.git"
    dest_path: "/opt/project"
    branch: "main"

  roles:
    - deploy_git_repo
